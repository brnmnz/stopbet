import SwiftUI

struct ConfiguracoesView: View {
    var body: some View {
        VStack {
            Spacer()
            Text("Em breve você poderá ajustar preferências aqui.")
                .multilineTextAlignment(.center)
                .foregroundColor(.gray)
                .padding()
            Spacer()
        }
    }
}
